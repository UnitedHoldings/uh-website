import React from 'react';
import ClaimsPage from '../../components/ClaimsPage';

export const metadata = {
  title: 'File & Track a Claim - United Holdings',
  description: 'Submit a claim or track an existing claim with United Holdings.'
};

export default function Page() {
  return <ClaimsPage />;
}
